name: "Greet"
description: "Checks out the repo and prints a greeting"

inputs:
  name:
    description: "who to greet"
    required: false
    default: "world"

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - run: echo "Hello, ${{ inputs.name }}!"
      shell: bash
