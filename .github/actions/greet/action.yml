name: "Greet"
description: "Prints a greeting"

inputs:
  name:
    description: "who to greet"
    required: false
    default: "world"

runs:
  using: "composite"
  steps:
    - run: echo "Hello, ${{ inputs.name }}!"
      shell: bash
